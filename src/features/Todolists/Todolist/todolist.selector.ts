import {AppRootStateType} from "app/store";

//export const selectTasks = (state: AppRootStateType) => state.tasks[props.id]
export const selectTodolists = (state: AppRootStateType) => state.todolists